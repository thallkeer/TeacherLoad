@model IEnumerable<TeacherLoad.Core.Models.GroupStudies>

@{
    ViewData["Title"] = "Index";
}
<h2>Виды групповых занятий</h2>
<p>
    <a class="btn btn-primary" asp-action="Create">Добавить вид занятий</a>
</p>
<table class="table table-striped">
    <thead class="thead-light">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GroupClassName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.GroupClassName)
                </td>
                <td>
                    <a class="btn-sm btn-success" asp-action="Edit" asp-route-id="@item.GroupClassID">
                        <span class="btn-label"><i class="glyphicon glyphicon-pencil"></i></span>
                        Редактировать
                    </a>
                    <a class="btn-sm btn-danger" asp-action="Delete" asp-route-id="@item.GroupClassID">
                        <span class="btn-label"><i class="glyphicon glyphicon-trash"></i></span>
                        Удалить
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>
